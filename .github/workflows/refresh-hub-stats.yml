name: Refresh hub-stats source

on:
  schedule:
    # Daily at 09:30 UTC — 30 min after refresh-models so a same-day
    # diff from each lands sequentially rather than racing the seed.yml
    # downstream trigger.
    - cron: "30 9 * * *"
  workflow_dispatch: {}

# Direct-commit auto-sync (no PR review). When HITL or consumer feedback
# loops come online we can flip to PR mode.
permissions:
  contents: write
  # `gh workflow run seed.yml` (downstream dispatch) requires actions:write.
  actions: write

env:
  GENERATED_PATH: seed/models/sources/hub_stats.generated.yaml

concurrency:
  group: refresh-hub-stats
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    # The DuckDB query against the remote parquet can take a few minutes;
    # 15 is generous and bounded.
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Sync dependencies
        run: uv sync

      - name: Refresh hub-stats generated source
        run: uv run python scripts/refresh_from_hub_stats.py

      # Sanity: the YAML must parse and the seed loader must succeed
      # before we commit. The seed merge has narrow contracts (parents
      # union by id, kind enum on orgs) — any malformed entry would
      # break the seed.yml pipeline that runs on every push to main.
      - name: Validate generated YAML loads
        run: |
          uv run python -c "import yaml; yaml.safe_load(open('$GENERATED_PATH'))"
          uv run eval-card-registry seed --local

      - name: Detect changes
        id: diff
        run: |
          if git diff --quiet "$GENERATED_PATH"; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "::notice::$GENERATED_PATH unchanged — nothing to commit."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            git diff --stat "$GENERATED_PATH"
          fi

      - name: Commit + push directly
        if: steps.diff.outputs.changed == 'true'
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$GENERATED_PATH"
          git commit -m "chore(seed): refresh hub-stats source ($(date -u +%Y-%m-%d))"
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"

      # GITHUB_TOKEN-authored pushes do NOT trigger downstream workflows.
      # Dispatch seed.yml explicitly so the refresh actually deploys.
      - name: Trigger seed deployment
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run seed.yml --ref "${GITHUB_REF_NAME}" || echo "::warning::seed.yml dispatch failed; trigger manually"

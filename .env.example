# Copy to .env and fill in values.

# HF Hub credentials and dataset repo
HF_TOKEN=
HF_DATASET_REPO=demo-org/demo-eval-card-registry # change to actual org and repo

# Set to true for local dev — loads from fixtures/ instead of HF Hub
LOCAL_MODE=false

# Resolver
RESOLVER_AUTO_MERGE_THRESHOLD=0.85

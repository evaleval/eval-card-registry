.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
dist/
build/

# Secrets
.env

# Sync output
output/

# Parquet files (fixtures are generated via `eval-card-registry seed --local`)
*.parquet
# ...except the tracked resolution oracle snapshot (the generator-layer
# re-architecture's Phase-4 diff target — see specs/model-resolution-rework).
!curation/oracle_snapshot/*.parquet
# ...and the durable offline hub-stats enrichment cache (reproducible lineage
# regen without live HF — refreshed by scripts/freeze_hub_stats_cache.py).
!curation/hub_stats_frozen.parquet

# Claude Code
CLAUDE.md
.claude/

# macOS
.DS_Store

# Scratch HF Space clones (nested working copies)
registry-test/

# Local API smoke-test notebooks
deploy/smoke_test.ipynb
deploy/test.ipynb
deploy/.ipynb_checkpoints/

# Design specs (local-only)
specs/
deprecated_refs/
AGENTS.md
dep
# Resolution curation: inputs (denylist/index/proposed/rescues) are tracked;
# regenerable outputs (review + analysis artifacts) are not.
curation/org_unknown_review.json
curation/suspicious_aliases.json
curation/alias_sweep_result.json
